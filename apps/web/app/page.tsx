import Link from "next/link";
import { redirect } from "next/navigation";

import { getProfessionals, getReceptionAgenda, getReceptionNow, getUnits, type AgendaEntry, type Professional, type ReceptionResourceState } from "../lib/bcos-api";
import { auth } from "../lib/auth/server";

export const dynamic = "force-dynamic";

type DashboardData = { unitName: string; timezone: string; resources: ReceptionResourceState[]; professionals: Professional[]; agenda: AgendaEntry[]; error: string | null };
function localDayBounds(timeZone: string): { startsAt: string; endsAt: string } { const now=new Date(); const parts=new Intl.DateTimeFormat("en-CA",{timeZone,year:"numeric",month:"2-digit",day:"2-digit"}).formatToParts(now); const year=Number(parts.find(p=>p.type==="year")?.value),month=Number(parts.find(p=>p.type==="month")?.value),day=Number(parts.find(p=>p.type==="day")?.value); const noonUtc=new Date(Date.UTC(year,month-1,day,12)); const offset=new Intl.DateTimeFormat("en-US",{timeZone,timeZoneName:"longOffset"}).formatToParts(noonUtc).find(p=>p.type==="timeZoneName")?.value.replace("GMT","")||"+00:00"; const d=`${year.toString().padStart(4,"0")}-${month.toString().padStart(2,"0")}-${day.toString().padStart(2,"0")}`; return {startsAt:`${d}T00:00:00${offset}`,endsAt:`${d}T23:59:59${offset}`}; }
function formatTime(v:string,t:string){return new Intl.DateTimeFormat("pt-BR",{timeZone:t,hour:"2-digit",minute:"2-digit"}).format(new Date(v))} function formatDay(t:string){return new Intl.DateTimeFormat("pt-BR",{timeZone:t,weekday:"long",day:"2-digit",month:"long"}).format(new Date())} function cleanUnitName(n:string){return n.replace(/\s*-\s*homologa[cç][aã]o/gi,"").replace(/\s+homologa[cç][aã]o/gi,"").trim()} function label(v:string){return ({ACTIVE:"Ativo",AVAILABLE:"Livre",CONFIRMED:"Confirmado",PENDING:"Pendente",CHECKED_IN:"Em atendimento",COMPLETED:"Concluído",CANCELLED:"Cancelado",CLEANING:"Limpeza",MAINTENANCE:"Manutenção",BLOCKED:"Bloqueado",OCCUPIED:"Em uso"} as Record<string,string>)[v.toUpperCase()]??v.replaceAll("_"," ")} function tone(v:string){const s=v.toUpperCase();if(["AVAILABLE","ACTIVE","COMPLETED","CONFIRMED"].includes(s))return"positive";if(["PENDING","CLEANING"].includes(s))return"attention";if(["CHECKED_IN","OCCUPIED"].includes(s))return"live";if(["CANCELLED","BLOCKED","MAINTENANCE"].includes(s))return"critical";return"neutral"}
function Icon({type}:{type:"resource"|"person"|"search"|"calendar"|"clock"|"arrow"}){if(type==="resource")return <svg aria-hidden="true" viewBox="0 0 24 24"><path d="M4 20V8.8c0-.7.4-1.4 1-1.7l6-3.3c.6-.3 1.3-.3 1.9 0L19 7.1c.6.4 1 1 1 1.7V20M2 20h20M8 20v-5h8v5"/></svg>;if(type==="person")return <svg aria-hidden="true" viewBox="0 0 24 24"><path d="M12 12a4 4 0 1 0 0-8 4 4 0 0 0 0 8ZM4.5 20c.8-3.5 3.3-5.5 7.5-5.5s6.7 2 7.5 5.5"/></svg>;if(type==="search")return <svg aria-hidden="true" viewBox="0 0 24 24"><circle cx="11" cy="11" r="6.5"/><path d="m16 16 4 4"/></svg>;if(type==="calendar")return <svg aria-hidden="true" viewBox="0 0 24 24"><rect x="3" y="5" width="18" height="16" rx="3"/><path d="M8 3v4M16 3v4M3 10h18"/></svg>;if(type==="clock")return <svg aria-hidden="true" viewBox="0 0 24 24"><circle cx="12" cy="12" r="9"/><path d="M12 7v5l3 2"/></svg>;return <svg aria-hidden="true" viewBox="0 0 24 24"><path d="M5 12h14M14 7l5 5-5 5"/></svg>}
async function loadDashboardData():Promise<DashboardData>{try{const units=await getUnits();const unit=units.find(i=>i.active)??units[0];if(!unit)return{unitName:"Unidade",timezone:"America/Sao_Paulo",resources:[],professionals:[],agenda:[],error:null};const b=localDayBounds(unit.timezone);const[nowState,professionals,agenda]=await Promise.all([getReceptionNow(unit.id),getProfessionals(),getReceptionAgenda({unitId:unit.id,startsAt:b.startsAt,endsAt:b.endsAt})]);return{unitName:unit.name,timezone:unit.timezone,resources:nowState.resources,professionals,agenda,error:null}}catch(error){console.error("BCOS operational center load failed:",error);return{unitName:"Unidade",timezone:"America/Sao_Paulo",resources:[],professionals:[],agenda:[],error:"Não foi possível carregar a operação neste momento."}}}
export default async function HomePage(){const { data: session } = await auth.getSession();if(!session?.user)redirect("/auth/sign-in");const data=await loadDashboardData(),unitName=cleanUnitName(data.unitName),professionalById=new Map(data.professionals.map(i=>[i.id,i]));const activeUsage=data.agenda.find(e=>e.usage?.status.toUpperCase()==="CHECKED_IN")?.usage??null;const currentResource=activeUsage?data.resources.find(r=>r.resource_id===activeUsage.resource_id):data.resources.find(r=>r.usage_status?.toUpperCase()==="CHECKED_IN");const upcoming=data.agenda.filter(e=>e.booking.status.toUpperCase()==="CONFIRMED"&&e.usage===null).slice(0,5),completed=data.agenda.filter(e=>e.usage?.status.toUpperCase()==="COMPLETED").length;return <main className="product-shell"><aside className="product-rail"><div><div className="rail-brand"><div className="rail-brand-mark">H</div><div className="rail-brand-copy"><strong>HPTECH</strong><span>Beauty Coworking OS</span></div></div><nav className="rail-nav"><Link className="rail-link rail-link-active" href="/"><span className="rail-icon"><Icon type="clock"/></span>Agora</Link><Link className="rail-link" href="/agenda"><span className="rail-icon"><Icon type="calendar"/></span>Agenda</Link><Link className="rail-link" href="/#espacos"><span className="rail-icon"><Icon type="resource"/></span>Espaços</Link><Link className="rail-link" href="/#profissionais"><span className="rail-icon"><Icon type="person"/></span>Profissionais</Link><Link className="rail-link" href="/check-in"><span className="rail-icon"><Icon type="clock"/></span>Check-in</Link><Link className="rail-link" href="/financeiro"><span className="rail-icon"><Icon type="arrow"/></span>Financeiro</Link><Link className="rail-link" href="/profissional"><span className="rail-icon"><Icon type="person"/></span>Meu portal</Link></nav></div><div className="rail-bottom"><div className="tenant-signature"><span className="tenant-logo-placeholder">LB</span><div><strong>{unitName}</strong><span>Ambiente operacional</span></div></div><div className="powered-by"><span>Produto</span><strong>HPTECH PLATFORM</strong></div></div></aside><section className="product-workspace"><header className="command-header"><div className="command-heading"><span>Central da Recepção</span><strong>{unitName}</strong></div><button className="global-command" type="button"><span className="global-command-icon"><Icon type="search"/></span><span className="global-command-label">Buscar pessoa, reserva ou espaço</span><kbd>Ctrl K</kbd></button><div className="operator"><div className="operator-copy"><strong>Recepção</strong><span>Operação do dia</span></div></div></header><div className="operational-canvas"><section className="day-heading"><div><span className="day-kicker">{formatDay(data.timezone)}</span><h1>O que está acontecendo agora.</h1></div><div className={data.error?"live-health live-health-error":"live-health"}><span className="live-health-dot"/><div><strong>{data.error?"Operação indisponível":"Operação conectada"}</strong><span>{data.error??`${data.agenda.length} reservas hoje`}</span></div></div></section><section className="now-layout"><article className="now-card"><div className="section-heading"><div><span className="section-eyebrow">AGORA</span><h2>Ocupação física real</h2></div><span className="section-live-badge">AO VIVO</span></div>{activeUsage&&currentResource?<div className="current-operation"><div className="current-time"><span>CHECK-IN</span><strong>{activeUsage.checked_in_at?formatTime(activeUsage.checked_in_at,data.timezone):"—"}</strong></div><div className="current-copy"><span className="current-label">Profissional</span><strong>{professionalById.get(activeUsage.professional_id)?.name??"Profissional"}</strong><div className="current-meta">{currentResource.resource_name}</div></div><div className="current-action"><span className="status-pill status-live">Em atendimento</span><Link className="primary-action" href={`/operacao?usage=${activeUsage.id}`}>Abrir operação</Link></div></div>:<div className="operational-empty operational-empty-calm"><div className="empty-symbol"><Icon type="clock"/></div><div><strong>Nenhum atendimento com check-in em curso</strong><p>Quando um profissional iniciar o uso, a ocupação aparecerá aqui em tempo real.</p></div></div>}</article><aside className="pulse-card"><span className="section-eyebrow">HOJE</span><div className="pulse-number"><strong>{data.agenda.length}</strong><span>reservas</span></div><div className="pulse-row"><span>Concluídas</span><strong>{completed}</strong></div><div className="pulse-row"><span>Próximas</span><strong>{upcoming.length}</strong></div><div className="pulse-row"><span>Espaços ativos</span><strong>{data.resources.length}</strong></div></aside></section><section className="agenda-section"><div className="section-heading"><div><span className="section-eyebrow">PRÓXIMOS</span><h2>Reservas aguardando check-in</h2></div><Link className="text-action" href="/agenda">Ver agenda completa</Link></div>{upcoming.length===0?<div className="quiet-state"><div><strong>Nenhuma reserva aguardando check-in</strong><span>A agenda está livre neste momento.</span></div></div>:<div className="agenda-list">{upcoming.map(({booking,usage})=><article className="agenda-row" key={booking.id}><div className="agenda-time"><strong>{formatTime(booking.starts_at,data.timezone)}</strong><span>{formatTime(booking.ends_at,data.timezone)}</span></div><div className="agenda-profile"><div><strong>{professionalById.get(booking.professional_id)?.name??"Profissional"}</strong><span>{data.resources.find(r=>r.resource_id===booking.resource_id)?.resource_name??"Espaço"}</span></div></div><span className={`status-pill status-${tone(usage?.status??booking.status)}`}>{label(usage?.status??booking.status)}</span></article>)}</div>}</section><section className="spaces-section" id="espacos"><div className="section-heading"><div><span className="section-eyebrow">ESPAÇOS</span><h2>O coworking agora</h2></div><span className="section-count">{data.resources.length} ativos</span></div><div className="resource-grid">{data.resources.map(resource=>{const state=resource.usage_status==="CHECKED_IN"?"OCCUPIED":resource.operational_status;return <article className="resource-card" key={resource.resource_id}><div className="resource-visual"><div className="resource-visual-icon"><Icon type="resource"/></div><span className={`status-pill status-${tone(state)}`}>{label(state)}</span></div><div className="resource-body"><span className="resource-kicker">ESPAÇO</span><strong>{resource.resource_name}</strong><div className="resource-next"><span>AGORA</span><strong>{resource.booking_id?"Reserva ativa":"Sem ocupação"}</strong></div></div></article>})}</div></section><section className="people-section" id="profissionais"><div className="section-heading"><div><span className="section-eyebrow">PROFISSIONAIS</span><h2>Profissionais ativos</h2></div></div><div className="people-grid">{data.professionals.map(p=><article className="person-card" key={p.id}><div className="person-copy"><strong>{p.name}</strong><span>{p.email??"Profissional do coworking"}</span><div className="person-next"><span>STATUS</span><strong>{label(p.status)}</strong></div></div></article>)}</div></section></div></section></main>}
